---
import Layout from '../layouts/Layout.astro';
import StatsBar from '../components/islands/StatsBar';
import CarNumberSearchBar from '../components/islands/CarNumberSearchBar';
import VehicleGrid from '../components/islands/VehicleGrid';
---

<Layout title="공차경매 - 공공기관 차량 경매">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Hero Section -->
    <section class="mb-8">
      <div class="card bg-gradient-to-r from-primary-600 to-primary-700 text-white p-6 sm:p-8 border-0">
        <h1 class="text-2xl sm:text-3xl font-bold mb-2">공공기관 차량 경매</h1>
        <p class="text-primary-100 text-sm sm:text-base max-w-2xl">
          온비드 및 각 공공기관에서 진행하는 차량 경매 정보를 한눈에 확인하세요.
          합리적인 가격에 관용차를 구매할 수 있는 기회입니다.
        </p>
        <div class="flex flex-wrap gap-4 mt-6">
          <div class="flex items-center gap-2">
            <div class="w-10 h-10 rounded-full bg-white/20 flex items-center justify-center">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <div>
              <p class="text-xs text-primary-200">실시간 업데이트</p>
              <p class="text-sm font-medium">매일 자동 수집</p>
            </div>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-10 h-10 rounded-full bg-white/20 flex items-center justify-center">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z" />
              </svg>
            </div>
            <div>
              <p class="text-xs text-primary-200">상세 필터</p>
              <p class="text-sm font-medium">원하는 조건 검색</p>
            </div>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-10 h-10 rounded-full bg-white/20 flex items-center justify-center">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
            </div>
            <div>
              <p class="text-xs text-primary-200">알림 서비스</p>
              <p class="text-sm font-medium">신규 등록 알림</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Stats Bar (React Island) -->
    <StatsBar client:load />

    <!-- Car Number Search Bar -->
    <CarNumberSearchBar client:load />

    <!-- Vehicle Grid (React Island) -->
    <VehicleGrid client:load />
  </div>
</Layout>
